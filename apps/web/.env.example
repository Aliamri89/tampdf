# Long random string used to sign Payload CMS auth tokens. Generate one with
# `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
# and never reuse the same value across environments.
PAYLOAD_SECRET=

# SQLite database file used by Payload CMS (libSQL client URL format).
DATABASE_URI=file:./tampdf.db
