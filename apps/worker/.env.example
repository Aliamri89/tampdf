PORT=8787
HOST=0.0.0.0

# Shared secret required in the x-api-key header. Must match WORKER_API_KEY
# in the web app's environment. Leave unset to disable auth (local dev only).
WORKER_API_KEY=

MAX_FILE_SIZE_BYTES=26214400
